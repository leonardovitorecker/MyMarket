@model MyMarket.Models.Estoque

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Estoque</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="estoqueAtual" class="control-label"></label>
                <input asp-for="estoqueAtual" class="form-control" />
                <span asp-validation-for="estoqueAtual" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="produtoid" class="control-label">Produto</label>
                @*HtmlHelpers*@
                @*<text>Local</text> @Html.DropDownList("Produto", new SelectList(ViewBag.Produto))*@
                @*Razor*@
                <select asp-items=@ViewBag.Produto2 asp-for="produtoid" class="form-control"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
